

class LogController {
    /**
     * @method newLog
     */
    async newLog(req, res) {
        try {
            
        } catch (error) {
            console.log("Error while newLog api calling : ", error.message);
        }
    }
}